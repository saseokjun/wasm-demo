# Webassembly in react application
## commands
```
npx create-react-app <PROJECT_NAME> --template typescript
cd <PROJECT_NAME>
yarn add typescript @types/node @types/react @types/react-dom
yarn add assemblyscript --dev
npx asinit .

```